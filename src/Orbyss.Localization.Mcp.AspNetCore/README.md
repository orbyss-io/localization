# Orbyss.Localization.Mcp.AspNetCore

An independently selected `IShellFeature` that contributes explicit localization management and
runtime-inspection tools to the shared authenticated `Orbyss.Foundation.Mcp.AspNetCore` transport. It maps
no endpoint or middleware and derives all mutation actors from the validated principal.
