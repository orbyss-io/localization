# Orbyss.Localization.Management.Mcp.AspNetCore

An independently selected `IShellFeature` that contributes governed localization catalog and
release-management tools to the shared authenticated `Orbyss.Foundation.Mcp.AspNetCore` transport.
It maps no endpoint or middleware and derives every mutation actor from the validated principal.
Read-only runtime tools remain separately selectable.
