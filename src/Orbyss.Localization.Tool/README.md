# Orbyss.Localization.Tool

Explicit closed-world MCP tools over the same catalog, import/export, lifecycle, release-diff, and
runtime services used by the Localization HTTP features. Import/export transfer is base64 encoded
and strictly bounded. Exact idempotency inputs are explicit and actors come only from the validated
transport principal.
